# Email Form

This static site includes a Netlify Forms-powered contact form. Submissions are captured by Netlify and can be sent to a Google email account through Netlify's form notification settings.

After deploy, open the site's Netlify dashboard and add an email notification for the `contact` form using the Google account address that should receive responses. This keeps mail delivery credentials out of the repository.
